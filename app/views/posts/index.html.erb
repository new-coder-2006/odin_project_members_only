<h1>Posts</h1>

<% @posts.each do |post| %>
  <div class="post">
    <h2><%= post.title %></h2>
    <p><%= post.body %></p>
    <% if user_signed_in? %>
        <p><%= User.find(post.user_id).email %></p>
    <% else %>
        <p>Anonymous</p>
    <% end %>
  </div>
<% end %>